apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: iron-bank-certs
spec:
  secretName: iron-bank-tls
  privateKey:
    algorithm: ECDSA
    size: 256
  dnsNames:
    - "iron-bank.app.tribuz.com.br"
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer